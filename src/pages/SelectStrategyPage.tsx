import React, { FC } from 'react';

type Props = {};

const SelectStrategyPage: FC<Props> = props => {
  return (
    <div>
      <span>Select this page</span>
    </div>
  );
};

export default SelectStrategyPage;
